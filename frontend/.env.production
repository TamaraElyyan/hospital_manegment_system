# Used by: npm run build, Vite production mode (e.g. Render static site)
# Backend on Railway — no trailing slash, no /api (axios adds /api)
VITE_API_URL=https://hospital-api-production-d897.up.railway.app
