# Build-time: Vite embeds this at npm run build (see .env.production in repo for current deploy).
# No trailing slash, no /api (axios appends /api in src/api/axios.js).
# VITE_API_URL=https://hospital-api-production-d897.up.railway.app
VITE_API_URL=
